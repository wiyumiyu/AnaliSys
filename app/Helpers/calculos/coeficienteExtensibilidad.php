<?php

namespace App\Helpers\Calculos;

class coeficienteExtensibilidad {

    /* public static function calcular_volumen(
        $diametro_muestra,
        $altura_cilindro,
        $diametro_cilindro
    ) {
        // Fórmula para calcular el volumen del cilindro
        $radio_cilindro = $diametro_cilindro / 2;
        $volumen = pi() * pow($radio_cilindro, 2) * $altura_cilindro;

        return round($volumen, 3); // Redondear a 3 decimales
    }
       
    ) */        
}